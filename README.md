# pyspark-word-count

Local env:
1. Install pipenv
    ```bash
    brew install pipenv
    ```

2. Install packages:
    ```bash
    pipenv install
    ```

3. Activate virtualenv
    ```bash
    pipenv shell
    ```